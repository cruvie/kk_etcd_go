syntax = "proto3";
package api_def;
option go_package = "/api_def";
//add user
message UserAdd {
  message Input {
    string  UserName = 2;
    string  Password = 3;
    repeated  string  Roles = 4;
  }

  message Output {
  }
}