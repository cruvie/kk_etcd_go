syntax = "proto3";
package snapshotInfo;
option go_package = "/snapshotInfo";

//snapshot info
message SnapshotInfo {
  message Input {
    bytes File = 2;
  }

  message Output {
    string  Info = 1;
  }
}